package filters

import (
	"fmt"
	"golang.org/x/exp/slices"
)

templ FilterByOSVersions(f AgentFilter, url, target, swap string, versions []string) {
	<button title="Filter by OS Version" type="button">
		if len(f.OSVersions) > 0 {
			<uk-icon icon="filter-x" hx-history="false" custom-class="mr-2 h-5 w-5 fill-red-500 text-red-500" uk-cloak></uk-icon>
		} else {
			<uk-icon icon="filter" hx-history="false" custom-class="mr-2 h-5 w-5" uk-cloak></uk-icon>
		}
	</button>
	<div class="uk-drop uk-dropdown" uk-dropdown="mode: click">
		<ul class="uk-dropdown-nav uk-nav">
			for index, version := range versions {
				<li class="p-2">
					<input
						id={ fmt.Sprintf("filterByOSVersion%d", index) }
						name={ fmt.Sprintf("filterByOSVersion%d", index) }
						type="checkbox"
						value={ version }
						class="uk-checkbox mx-3"
						hx-post={ url }
						hx-push-url="false"
						hx-target={ target }
						hx-swap={ swap }
						checked?={ slices.Contains(f.OSVersions, version) }
					/>
					<label for={ fmt.Sprintf("filterByOSVersion%d", index) }>{ version }</label>
				</li>
			}
		</ul>
	</div>
}
